#include "main.h"

/**
 * print_last_digit - prints the last digit of the number
 *
 * Return: 0
 */

int print_last_digit(int m, y)
{
	y = m % 10;
	
	if (y = m % 10)
	       _putchar(y);

	return (0);
}	
