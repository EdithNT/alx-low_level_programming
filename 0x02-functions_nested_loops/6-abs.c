#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @a: integer value whise abs value we are calculating
 *
 * Return: 0
 */

int _abs(int a)
{
	if (a >= 0)
		return (a);
	else
		return (-a);
}
