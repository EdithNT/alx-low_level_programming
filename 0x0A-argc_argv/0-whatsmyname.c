#include <stdio.h>

/**
 * main - entry point
 * @argc: first argument
 * @argv: second argument to print an array
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
