#include <stdio.h>

/**
 * main - entry point
 * @argc: first argument
 * @argv: second argument
 *
 * Return: 0
 */

int main(argc,_attribute_((unused))argv[])
{
	printf("argc = %d\n", argc);

	return (0);
}
