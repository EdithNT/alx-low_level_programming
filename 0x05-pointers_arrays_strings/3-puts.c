#include "main.h"

/**
 * _puts - print a string, followed by a newline, to stdout
 * @str: value of string
 * Return: void
 */

void _puts(char *str)
{
	char *str;

	while (*str)
		_putchar(*s++);
}
