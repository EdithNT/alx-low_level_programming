#include "main.h"

/**
 * _isupper - we are checking for upper case letters
 *
 * Return: 1 if uppercase, 0 otherwise
 */

int _isupper(int c);
{
	if (c >= 'A'; c <= 'Z')
	{
		return (1);
	return (0);
	}
}
